Quantum ESPRESSO-specific example codes, archives, and bootstrap files live here.

These files are intentionally kept out of the worker root and generic `data/`
paths so the default package layout stays plugin-agnostic.
